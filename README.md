# rp2040-qteloader
Minimal working version of bootloader that allows for storing multiple binaries on flash and executing them.
Also allows for scanning flash for rp2040 c/c++ sdk compiled binaries, and storing their addreses in flash.
